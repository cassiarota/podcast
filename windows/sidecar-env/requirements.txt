# Qwen TTS sidecar deps (Windows + CUDA only)
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
numpy>=1.26
soundfile>=0.12

# CUDA build of PyTorch — adjust the index URL to match the host driver.
# pip install --index-url https://download.pytorch.org/whl/cu121 torch
torch>=2.4

# Qwen TTS package
qwen-tts>=0.1
